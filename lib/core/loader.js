'use strict'

/**
 * 加载启动对应的service, model , controller;
 */


module.exports = class loader {
    constructor(app , options = {}) {
        this.app = app;
        this.options = options;
    }

    // 加载全部的loader;
    get loader(){

    }

    // register 对应的服务;
    register(){
        
    }

}



